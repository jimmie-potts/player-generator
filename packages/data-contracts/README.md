# Data-contracts package

This Python package owns shared versioned schema resources, rating-field identifiers, tier ordering,
and validation for existing data interfaces. The current roster JSON schema is packaged as
`schemas/roster-v1.schema.json`.

Normalized reference and roster CSV contracts will be added by US-005 and US-009. Their planned
headers are documentation targets until those stories are complete.
